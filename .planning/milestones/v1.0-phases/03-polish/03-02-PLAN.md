---
phase: 03-polish
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - internal/core/ports/file_service.go
  - internal/core/ports/file_service_test.go
  - internal/adapters/data/sftp_client/sftp_client.go
  - internal/adapters/data/sftp_client/sftp_client_test.go
  - internal/core/domain/transfer.go
  - internal/core/ports/transfer.go
  - internal/adapters/data/transfer/transfer_service.go
  - internal/adapters/data/transfer/transfer_service_test.go
  - internal/adapters/ui/file_browser/transfer_modal.go
  - internal/adapters/ui/file_browser/file_browser.go
autonomous: true
requirements: [TRAN-07]
user_setup: []

must_haves:
  truths:
    - "用户按 y/Enter/Esc 确认取消后，传输停止且目标侧部分文件被删除（D-04）"
    - "上传文件时，如果远程目标文件已存在，modal 显示冲突对话框提供 Overwrite/Skip/Rename 选项"
    - "下载文件时，如果本地目标文件已存在，modal 显示冲突对话框提供 Overwrite/Skip/Rename 选项"
    - "用户选择 Skip 后文件被跳过，状态栏显示 'Skipped: filename'"
    - "用户选择 Rename 后文件以 file.1.txt 格式重命名传输，状态栏显示 'Renamed to: file.1.txt'"
    - "目录传输中每个冲突文件单独提示（非 apply-all）"
    - "冲突对话框显示在 TransferModal 区域内（替换进度显示），不切换到单独的 view"
  artifacts:
    - path: "internal/core/ports/file_service.go"
      provides: "SFTPService 接口新增 Stat/Remove 方法"
      contains: "Stat(path string) (os.FileInfo, error)"
    - path: "internal/adapters/data/sftp_client/sftp_client.go"
      provides: "Stat/Remove 的 SFTPClient 实现"
      contains: "func (c *SFTPClient) Stat(path string)"
    - path: "internal/adapters/data/transfer/transfer_service.go"
      provides: "传输前冲突检测、onConflict 回调、取消后部分文件清理"
      contains: "onConflict"
    - path: "internal/adapters/ui/file_browser/transfer_modal.go"
      provides: "modeConflictDialog 冲突对话框渲染和键盘处理"
      contains: "ShowConflict"
    - path: "internal/adapters/ui/file_browser/file_browser.go"
      provides: "冲突回调的 goroutine 同步实现（actionCh channel）"
      contains: "ConflictAction"
  key_links:
    - from: "file_browser.go"
      to: "transfer_service.go"
      via: "onConflict 回调函数通过 actionCh channel 同步"
      pattern: "ConflictAction"
    - from: "transfer_service.go"
      to: "sftp_client.go"
      via: "sftp.Stat() 检测远程文件存在、sftp.Remove() 清理部分文件"
      pattern: "ts.sftp.Stat"
    - from: "transfer_modal.go"
      to: "file_browser.go"
      via: "HandleKey 发送 action 到 actionCh，goroutine 解除阻塞"
      pattern: "actionCh"
---

<objective>
添加 SFTPService Stat/Remove 方法，实现传输前冲突检测和取消后部分文件清理（D-04），在 TransferModal 中显示冲突对话框提供 Overwrite/Skip/Rename 三选一，并通过 channel 实现 goroutine 同步。

**为什么：** Plan 01 实现了 context 取消传播但未实现 D-04 的部分文件清理（依赖 SFTPService.Remove()）。此 Plan 添加 Stat/Remove 方法后，同时完成冲突检测和清理两个能力。冲突检测 + 对话框让用户对覆盖操作有完全控制权，避免意外覆盖重要文件。目录传输中的逐文件提示确保每个冲突都得到审慎处理（D-09）。

**输出：** SFTPService 接口新增 Stat/Remove 方法，TransferService 传输前检测冲突并通过回调请求用户决定，取消后清理部分文件（D-04），TransferModal 新增冲突对话框模式，FileBrowser 通过 actionCh channel 实现传输 goroutine 和 UI 线程的同步。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-polish/03-CONTEXT.md
@.planning/phases/03-polish/03-RESEARCH.md
@.planning/phases/03-polish/03-UI-SPEC.md
@.planning/phases/03-polish/03-01-PLAN.md

<!-- Plan 01 完成后的接口状态 -->
<!-- TransferService 接口已有 ctx context.Context 参数（无 onConflict） -->
<!-- TransferModal 已有 modalMode 枚举和 modeProgress/modeCancelConfirm/modeSummary -->
<!-- FileBrowser 已有 transferCancel context.CancelFunc -->
<!-- 取消后返回 context.Canceled 但未做文件清理（D-04 留待此 Plan） -->

<interfaces>
<!-- SFTPService 当前接口（Plan 02 添加 Stat/Remove） -->
From internal/core/ports/file_service.go:
```go
type SFTPService interface {
    FileService
    Connect(server domain.Server) error
    Close() error
    IsConnected() bool
    CreateRemoteFile(path string) (io.WriteCloser, error)
    OpenRemoteFile(path string) (io.ReadCloser, error)
    MkdirAll(path string) error
    WalkDir(path string) ([]string, error)
}
```

<!-- TransferService 当前接口（Plan 01 已添加 ctx） -->
From internal/core/ports/transfer.go (after Plan 01):
```go
type TransferService interface {
    UploadFile(ctx context.Context, localPath, remotePath string, onProgress func(domain.TransferProgress)) error
    DownloadFile(ctx context.Context, remotePath, localPath string, onProgress func(domain.TransferProgress)) error
    UploadDir(ctx context.Context, localPath, remotePath string, onProgress func(domain.TransferProgress)) ([]string, error)
    DownloadDir(ctx context.Context, remotePath, localPath string, onProgress func(domain.TransferProgress)) ([]string, error)
}
```

<!-- TransferProgress 当前结构 -->
From internal/core/domain/transfer.go:
```go
type TransferProgress struct {
    FileName   string; FilePath string; BytesDone int64; BytesTotal int64
    Speed float64; FileIndex int; FileTotal int; IsDir bool
    Done bool; Failed bool; FailError string
}
```

<!-- SFTPClient 内部持有 *sftp.Client -->
<!-- pkg/sftp.Client 具有 Stat(path string) (os.FileInfo, error) 和 Remove(path string) error -->
<!-- sftp_client.go 中使用 c.mu.Lock() 保护 c.client 访问 -->

<!-- TransferModal 当前结构（Plan 01 后） -->
<!-- 已有 modalMode 枚举: modeProgress, modeCancelConfirm, modeConflictDialog, modeSummary -->
<!-- 已有 HandleKey 根据 mode 分发 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SFTPService 添加 Stat/Remove 方法并更新 mock</name>
  <files>
    internal/core/ports/file_service.go
    internal/core/ports/file_service_test.go
    internal/adapters/data/sftp_client/sftp_client.go
    internal/adapters/data/sftp_client/sftp_client_test.go
  </files>
  <read_first>
    - internal/core/ports/file_service.go (SFTPService 接口)
    - internal/core/ports/file_service_test.go (mockSFTPService)
    - internal/adapters/data/sftp_client/sftp_client.go (SFTPClient 实现，注意 c.mu.Lock 模式)
    - internal/adapters/data/sftp_client/sftp_client_test.go (现有测试)
  </read_first>
  <behavior>
    - 测试 1: SFTPClient.Stat 存在的文件返回 FileInfo
    - 测试 2: SFTPClient.Stat 不存在的文件返回 error
    - 测试 3: SFTPClient.Remove 删除文件成功
  </behavior>
  <action>
    **设计原理：** Stat/Remove 是冲突检测和部分文件清理的基础操作。SFTPService 接口新增这两个方法，使 TransferService 可以在不依赖具体实现的情况下进行存在性检查和文件清理。遵循现有 SFTPClient 的 c.mu.Lock 模式确保线程安全。

    **实现步骤：**

    1. **在 `ports/file_service.go` 的 SFTPService 接口中添加 Stat 和 Remove**：
       ```go
       // Stat returns file info for the given remote path.
       // Returns error if the file does not exist.
       Stat(path string) (os.FileInfo, error)
       // Remove deletes the remote file or empty directory.
       Remove(path string) error
       ```
       添加 `"os"` import。

    2. **在 `sftp_client.go` 中实现 Stat 和 Remove**：
       ```go
       func (c *SFTPClient) Stat(path string) (os.FileInfo, error) {
           c.mu.Lock()
           client := c.client
           c.mu.Unlock()
           if client == nil {
               return nil, fmt.Errorf("not connected: call Connect first")
           }
           return client.Stat(path)
       }

       func (c *SFTPClient) Remove(path string) error {
           c.mu.Lock()
           client := c.client
           c.mu.Unlock()
           if client == nil {
               return fmt.Errorf("not connected: call Connect first")
           }
           return client.Remove(path)
       }
       ```
       遵循现有方法模式（c.mu.Lock -> 获取 client -> c.mu.Unlock -> 检查 nil -> 执行操作）。

    3. **更新 `file_service_test.go` 中的 mockSFTPService**：
       ```go
       func (m *mockSFTPService) Stat(path string) (os.FileInfo, error) {
           return nil, fmt.Errorf("not found")
       }
       func (m *mockSFTPService) Remove(path string) error {
           return nil
       }
       ```
       添加 `"os"` import。更新 TestSFTPServiceInterface 测试中的方法列表添加 "Stat", "Remove"。

    4. **添加 SFTPClient 单元测试**：
       在 `sftp_client_test.go` 中添加 Stat/Remove 测试（使用已连接的 client mock）。
  </action>
  <verify>
    <automated>cd /home/huanglin/code/lazyssh && go build ./internal/core/ports/... && go build ./internal/adapters/data/sftp_client/... && go test ./internal/core/ports/... ./internal/adapters/data/sftp_client/... -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `internal/core/ports/file_service.go` 包含 `Stat(path string) (os.FileInfo, error)`
    - `internal/core/ports/file_service.go` 包含 `Remove(path string) error`
    - `internal/adapters/data/sftp_client/sftp_client.go` 包含 `func (c *SFTPClient) Stat(`
    - `internal/adapters/data/sftp_client/sftp_client.go` 包含 `func (c *SFTPClient) Remove(`
    - `go vet ./internal/core/ports/... ./internal/adapters/data/sftp_client/...` 退出码为 0
  </acceptance_criteria>
  <done>SFTPService 接口新增 Stat/Remove 方法，SFTPClient 实现完成，mockSFTPService 同步更新，单元测试覆盖 Stat/Remove 基本场景。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TransferService 冲突检测 + 取消清理 + TransferModal 冲突对话框</name>
  <files>
    internal/core/domain/transfer.go
    internal/core/ports/transfer.go
    internal/adapters/data/transfer/transfer_service.go
    internal/adapters/data/transfer/transfer_service_test.go
    internal/adapters/ui/file_browser/transfer_modal.go
    internal/adapters/ui/file_browser/file_browser.go
  </files>
  <read_first>
    - internal/core/domain/transfer.go (添加 ConflictAction 枚举)
    - internal/core/ports/transfer.go (Plan 01 后版本，添加 onConflict 参数)
    - internal/adapters/data/transfer/transfer_service.go (Plan 01 后版本，已有 ctx 参数)
    - internal/adapters/data/transfer/transfer_service_test.go (mockSFTPService 需要更新)
    - internal/adapters/ui/file_browser/transfer_modal.go (Plan 01 后版本，已有 modalMode)
    - internal/adapters/ui/file_browser/file_browser.go (Plan 01 后版本，已有 ctx/cancel)
    - .planning/phases/03-polish/03-UI-SPEC.md (Mode 3: Conflict Dialog, Goroutine Synchronization Contract)
    - .planning/phases/03-polish/03-RESEARCH.md (Pattern 2: TransferModal 多模式切换, Pitfall 3: goroutine 同步死锁)
  </read_first>
  <behavior>
    - 测试 1: UploadFile 目标存在时调用 onConflict 回调
    - 测试 2: UploadFile onConflict 返回 ConflictSkip 时跳过文件
    - 测试 3: UploadFile onConflict 返回 ConflictRename 时使用新路径
    - 测试 4: UploadFile onConflict 返回 ConflictOverwrite 时继续传输
    - 测试 5: DownloadFile 目标存在时调用 onConflict 回调
    - 测试 6: UploadFile 上下文取消后清理部分远程文件（sftp.Remove）-- D-04
    - 测试 7: DownloadFile 上下文取消后清理部分本地文件（os.Remove）-- D-04
    - 测试 8: nextAvailableName 正确生成递增后缀
  </behavior>
  <action>
    **设计原理：** 冲突检测需要知道目标文件是否存在。Task 1 已添加 SFTPService.Stat/Remove，此 Task 将其集成到 TransferService 的传输流程中。冲突处理通过 `onConflict` 回调实现——TransferService 保持无状态，冲突决策由调用方（FileBrowser）通过 UI 收集后通过回调传递。取消后的部分文件清理（D-04）也在此实现——Plan 01 已让 copyWithProgress 返回 context.Canceled，此 Task 在 UploadFile/DownloadFile 中接收到该错误后调用 Remove 清理部分文件。

    **实现步骤：**

    1. **在 `domain/transfer.go` 中添加 ConflictAction 枚举**：
       ```go
       type ConflictAction int
       const (
           ConflictOverwrite ConflictAction = iota
           ConflictSkip
           ConflictRename
       )
       ```

    2. **修改 `ports/transfer.go` 接口** -- 添加 onConflict 回调参数：
       ```go
       type ConflictHandler func(fileName string) (ConflictAction, string)
       // ConflictHandler returns action and new path (for Rename)

       UploadFile(ctx context.Context, localPath, remotePath string, onProgress func(domain.TransferProgress), onConflict ConflictHandler) error
       DownloadFile(ctx context.Context, remotePath, localPath string, onProgress func(domain.TransferProgress), onConflict ConflictHandler) error
       UploadDir(ctx context.Context, localPath, remotePath string, onProgress func(domain.TransferProgress), onConflict ConflictHandler) ([]string, error)
       DownloadDir(ctx context.Context, remotePath, localPath string, onProgress func(domain.TransferProgress), onConflict ConflictHandler) ([]string, error)
       ```
       添加 `"github.com/Adembc/lazyssh/internal/core/domain"` import。

    3. **修改 `transfer_service.go` 实现**：

       **3a. UploadFile 添加冲突检测和取消清理（D-04）**：
       ```go
       func (ts *transferService) UploadFile(ctx context.Context, localPath, remotePath string,
           onProgress func(domain.TransferProgress), onConflict domain.ConflictHandler) error {
           // 冲突检测（D-06）
           if onConflict != nil {
               if _, err := ts.sftp.Stat(remotePath); err == nil {
                   action, newPath := onConflict(filepath.Base(remotePath))
                   switch action {
                   case domain.ConflictSkip:
                       return nil
                   case domain.ConflictRename:
                       remotePath = newPath
                   case domain.ConflictOverwrite:
                       // 继续使用原路径
                   }
               }
           }
           // ... 现有 open localFile, stat, createRemoteFile 逻辑 ...

           // 不使用 defer 关闭 remoteFile，改为显式关闭
           err = ts.copyWithProgress(ctx, localFile, remoteFile, localPath, localPath, total, onProgress)
           remoteFile.Close() // 先关闭再清理

           // D-04: 取消后清理部分文件（Plan 01 留待此 Plan 实现）
           if err == context.Canceled {
               ts.log.Infow("transfer canceled, cleaning up partial file", "path", remotePath)
               if removeErr := ts.sftp.Remove(remotePath); removeErr != nil {
                   ts.log.Warnw("failed to cleanup partial remote file", "path", remotePath, "error", removeErr)
               }
               return context.Canceled
           }
           return err
       }
       ```

       **3b. DownloadFile 添加冲突检测和取消清理（D-04）**：
       ```go
       // 类似 UploadFile，但检测本地 os.Stat 和清理 os.Remove
       if onConflict != nil {
           if _, err := os.Stat(localPath); err == nil {
               action, newPath := onConflict(filepath.Base(localPath))
               switch action {
               case domain.ConflictSkip: return nil
               case domain.ConflictRename: localPath = newPath
               case domain.ConflictOverwrite: // 继续
               }
           }
       }
       // D-04 取消后清理：
       if err == context.Canceled {
           if removeErr := os.Remove(localPath); removeErr != nil {
               ts.log.Warnw("failed to cleanup partial local file", "path", localPath, "error", removeErr)
           }
           return context.Canceled
       }
       ```

       **3c. UploadDir/DownloadDir 传递 onConflict 到每个文件传输**：
       修改 `uploadSingleFile` 和 `downloadSingleFile` 签名添加 `onConflict` 参数，在每文件传输前检测冲突。

    4. **更新 `transfer_service_test.go` 中的 mockSFTPService**：
       - 添加 `statFile os.FileInfo` 和 `statErr error` 字段
       - 添加 `removedPaths []string` 字段（记录删除调用）
       - 实现 Stat 和 Remove 方法
       - 添加 `createdPaths map[string]bool` 字段（跟踪已创建的文件，用于 Stat 检测存在）
       - 修改 CreateRemoteFile mock：在 `createdPaths` 中记录路径
       - 更新所有现有测试调用添加 nil onConflict 参数
       - 添加冲突检测测试（mock stat 返回已存在文件）
       - 添加取消清理测试（验证 sftp.Remove/os.Remove 被调用）

    5. **在 `transfer_modal.go` 中实现 modeConflictDialog 的 Draw 渲染**（03-UI-SPEC Mode 3）：
       ```go
       case modeConflictDialog:
           // 冲突对话框布局
           row := y + 1
           tview.Print(screen, "File already exists:", x, row, width, tview.AlignCenter, conflictWarningColor)
           row++
           tview.Print(screen, tm.conflictFileInfo, x, row, width, tview.AlignCenter, tcell.Color255)
           row += 2
           tview.Print(screen, "[o] Overwrite  [s] Skip  [r] Rename", x, row, width, tview.AlignCenter, tcell.Color255)
       ```

       添加字段和常量：
       ```go
       const conflictWarningColor = tcell.ColorRGBTo256(255, 165, 0) // #FFA500 orange

       // TransferModal 新增字段
       conflictFileInfo string          // "filename.txt (1.2M, 2024-03-15)"
       conflictActionCh  chan domain.ConflictAction
       ```

       添加方法：
       ```go
       func (tm *TransferModal) ShowConflict(fileName, fileInfo string, actionCh chan domain.ConflictAction) {
           tm.mode = modeConflictDialog
           tm.conflictFileInfo = fmt.Sprintf("%s (%s)", fileName, fileInfo)
           tm.conflictActionCh = actionCh
       }
       func (tm *TransferModal) InConflictDialog() bool {
           return tm.mode == modeConflictDialog
       }
       ```

       在 HandleKey 中添加 modeConflictDialog 处理：
       ```go
       case modeConflictDialog:
           switch event.Rune() {
           case 'o':
               if tm.conflictActionCh != nil { tm.conflictActionCh <- domain.ConflictOverwrite }
               tm.mode = modeProgress
               return nil
           case 's':
               if tm.conflictActionCh != nil { tm.conflictActionCh <- domain.ConflictSkip }
               tm.mode = modeProgress
               return nil
           case 'r':
               if tm.conflictActionCh != nil { tm.conflictActionCh <- domain.ConflictRename }
               tm.mode = modeProgress
               return nil
           }
           return nil
       ```

    6. **在 `file_browser.go` 中实现 onConflict 回调和 goroutine 同步**（03-UI-SPEC Goroutine Synchronization Contract）：

       在 `initiateTransfer()` 中创建 onConflict 回调：
       ```go
       onConflict := func(fileName string) (domain.ConflictAction, string) {
           actionCh := make(chan domain.ConflictAction, 1)

           var existingInfo string
           if fb.activePane == 0 {
               if fi, err := fb.sftpService.Stat(joinPath(fb.remotePane.GetCurrentPath(), fileName)); err == nil {
                   existingInfo = fmt.Sprintf("%s, %s", formatSize(fi.Size()), fi.ModTime().Format("2006-01-02 15:04"))
               }
           } else {
               if fi, err := os.Stat(filepath.Join(fb.localPane.GetCurrentPath(), fileName)); err == nil {
                   existingInfo = fmt.Sprintf("%s, %s", formatSize(fi.Size()), fi.ModTime().Format("2006-01-02 15:04"))
               }
           }

           fb.app.QueueUpdateDraw(func() {
               fb.transferModal.ShowConflict(fileName, existingInfo, actionCh)
           })

           action := <-actionCh

           switch action {
           case domain.ConflictSkip:
               fb.app.QueueUpdateDraw(func() {
                   fb.updateStatusBarTemp(fmt.Sprintf("[#FFA500]Skipped: %s[-]", fileName))
               })
               return action, ""
           case domain.ConflictRename:
               var newPath string
               if fb.activePane == 0 {
                   newPath = nextAvailableName(joinPath(fb.remotePane.GetCurrentPath(), fileName), fb.sftpService.Stat)
               } else {
                   newPath = nextAvailableName(filepath.Join(fb.localPane.GetCurrentPath(), fileName), os.Stat)
               }
               baseName := filepath.Base(newPath)
               fb.app.QueueUpdateDraw(func() {
                   fb.updateStatusBarTemp(fmt.Sprintf("[#FFA500]Renamed to: %s[-]", baseName))
               })
               return action, newPath
           default:
               fb.app.QueueUpdateDraw(func() {
                   fb.updateStatusBarTemp(fmt.Sprintf("[#FFA500]Overwrote: %s[-]", fileName))
               })
               return action, ""
           }
       }
       ```

       将 onConflict 传递给 transferSvc：
       ```go
       err = fb.transferSvc.UploadFile(ctx, localPath, remotePath, progressCb, onConflict)
       err = fb.transferSvc.DownloadFile(ctx, remotePath, localPath, progressCb, onConflict)
       ```

       在 `initiateDirTransfer()` 中同样实现 onConflict 回调。

    7. **在 `file_browser.go` 中添加 nextAvailableName 函数**：
       ```go
       func nextAvailableName(path string, statFunc func(string) (os.FileInfo, error)) string {
           ext := filepath.Ext(path)
           name := filepath.Base(path)
           dir := filepath.Dir(path)
           stem := name[:len(name)-len(ext)]
           for i := 1; i <= 100; i++ {
               candidate := filepath.Join(dir, fmt.Sprintf("%s.%d%s", stem, i, ext))
               if _, err := statFunc(candidate); err != nil {
                   return candidate
               }
           }
           return path
       }
       ```

    8. **导入 domain 包**：
       `transfer_modal.go` 中添加 `"github.com/Adembc/lazyssh/internal/core/domain"` import 用于 `domain.ConflictAction`。
  </action>
  <verify>
    <automated>cd /home/huanglin/code/lazyssh && go build ./internal/core/domain/... && go build ./internal/core/ports/... && go build ./internal/adapters/data/transfer/... && go build ./internal/adapters/ui/file_browser/... && go test ./internal/adapters/data/transfer/... -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `internal/core/domain/transfer.go` 包含 `ConflictAction`
    - `internal/core/domain/transfer.go` 包含 `ConflictOverwrite`
    - `internal/core/domain/transfer.go` 包含 `ConflictSkip`
    - `internal/core/domain/transfer.go` 包含 `ConflictRename`
    - `internal/core/ports/transfer.go` 包含 `ConflictHandler`
    - `internal/core/ports/transfer.go` 包含 `onConflict`
    - `internal/adapters/data/transfer/transfer_service.go` 包含 `ts.sftp.Stat`
    - `internal/adapters/data/transfer/transfer_service.go` 包含 `ts.sftp.Remove` (D-04 cleanup)
    - `internal/adapters/data/transfer/transfer_service.go` 包含 `os.Remove` (D-04 cleanup)
    - `internal/adapters/ui/file_browser/transfer_modal.go` 包含 `conflictActionCh chan domain.ConflictAction`
    - `internal/adapters/ui/file_browser/transfer_modal.go` 包含 `func (tm *TransferModal) ShowConflict(`
    - `internal/adapters/ui/file_browser/transfer_modal.go` 包含 `File already exists:`
    - `internal/adapters/ui/file_browser/transfer_modal.go` 包含 `[o] Overwrite  [s] Skip  [r] Rename`
    - `internal/adapters/ui/file_browser/file_browser.go` 包含 `actionCh := make(chan domain.ConflictAction, 1)`
    - `internal/adapters/ui/file_browser/file_browser.go` 包含 `<-actionCh`
    - `internal/adapters/ui/file_browser/file_browser.go` 包含 `nextAvailableName`
    - `internal/adapters/ui/file_browser/file_browser.go` 包含 `onConflict := func(`
    - `go vet ./...` 退出码为 0
  </acceptance_criteria>
  <done>TransferService 所有方法添加 onConflict 回调参数，UploadFile/DownloadFile 在传输前检测冲突并在取消后清理部分文件（D-04），TransferModal 支持冲突对话框模式（三选一按键），FileBrowser 通过 buffered channel 实现 goroutine 同步。</done>
</task>

</tasks>

<verification>
1. `go build ./...` 编译通过
2. `go vet ./...` 无警告
3. `go test ./internal/core/ports/... -v` 接口测试通过（含 Stat/Remove）
4. `go test ./internal/adapters/data/sftp_client/... -v` SFTPClient 测试通过（含 Stat/Remove）
5. `go test ./internal/adapters/data/transfer/... -v` 传输服务测试通过（含冲突和清理）
6. 代码审查：actionCh 使用 buffered channel (容量 1)
7. 代码审查：传输 goroutine 在 `<-actionCh` 阻塞（不阻塞 UI 线程）
8. 代码审查：remoteFile.Close() 在 sftp.Remove() 之前执行
9. 代码审查：HandleKey 在 modeConflictDialog 中消费所有按键（return nil）
</verification>

<success_criteria>
- [ ] SFTPService 接口有 Stat 和 Remove 方法
- [ ] SFTPClient 实现 Stat 和 Remove（遵循 c.mu.Lock 模式）
- [ ] mockSFTPService（file_service_test.go 和 transfer_service_test.go）都添加了 Stat/Remove
- [ ] TransferService 所有方法有 onConflict ConflictHandler 参数
- [ ] UploadFile 在传输前通过 sftp.Stat 检测远程冲突
- [ ] DownloadFile 在传输前通过 os.Stat 检测本地冲突
- [ ] D-04: 取消后 UploadFile 通过 sftp.Remove 清理部分远程文件
- [ ] D-04: 取消后 DownloadFile 通过 os.Remove 清理部分本地文件
- [ ] TransferModal modeConflictDialog 显示文件信息和三选一选项
- [ ] FileBrowser 通过 actionCh buffered channel 实现 goroutine 同步
- [ ] nextAvailableName 生成 file.1.txt 格式递增后缀
- [ ] 状态栏显示 Skipped/Renamed to/Overwrote 操作反馈
</success_criteria>

<output>
After completion, create `.planning/phases/03-polish/03-02-SUMMARY.md`
</output>
