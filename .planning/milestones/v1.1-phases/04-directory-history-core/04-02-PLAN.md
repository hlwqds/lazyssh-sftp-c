---
phase: 04-directory-history-core
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - internal/adapters/ui/file_browser/remote_pane.go
  - internal/adapters/ui/file_browser/file_browser.go
autonomous: true
requirements: [HIST-01, AUX-02]
user_setup: []

must_haves:
  truths:
    - "用户按 h 返回上级目录后，父目录路径被记录到最近目录列表（通过 onPathChange 回调链）"
    - "用户按 Enter 进入子目录后，子目录路径被记录到最近目录列表（已有行为，保持不变）"
    - "NavigateToParent() 调用后 app.Sync() 被执行（修复 ghost artifact 问题）"
    - "NavigateTo(path) 直接设置 currentPath 并 Refresh，不触发 onPathChange 回调"
  artifacts:
    - path: "internal/adapters/ui/file_browser/remote_pane.go"
      provides: "NavigateToParent 修复 + NavigateTo 方法"
      contains: "func (rp *RemotePane) NavigateTo("
      contains: "rp.onPathChange(rp.currentPath)"
    - path: "internal/adapters/ui/file_browser/file_browser.go"
      provides: "RecentDocs 字段 + build() 中的 Record 调用"
      contains: "recentDirs"
      contains: "recentDirs.Record("
  key_links:
    - from: "remote_pane.go NavigateToParent()"
      to: "file_browser.go build() onPathChange 回调"
      via: "onPathChange(rp.currentPath)"
      pattern: "onPathChange\\(rp\\.currentPath\\)"
    - from: "file_browser.go build()"
      to: "recent_dirs.go Record()"
      via: "fb.recentDirs.Record(path)"
      pattern: "recentDirs\\.Record\\("
    - from: "remote_pane.go NavigateTo()"
      to: "remote_pane.go Refresh()"
      via: "直接调用 Refresh，不触发 onPathChange"
      pattern: "func \\(rp \\*RemotePane\\) NavigateTo\\("
---

<objective>
修复 NavigateToParent 缺少 onPathChange 回调的 bug（AUX-02），添加 NavigateTo 方法供 Phase 5 使用，并在 FileBrowser.build() 中将 Record() 调用接入 onPathChange 回调链。

Purpose: 完成 HIST-01 的集成部分——通过修复 bug 使 NavigateToParent 也能触发路径记录，并通过回调链将所有导航事件连接到 RecentDirs.Record()。

Output: 修改 `remote_pane.go`（NavigateToParent 修复 + NavigateTo 新增）和 `file_browser.go`（RecentDirs 字段 + Record 接线）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-directory-history-core/04-CONTEXT.md
@.planning/phases/04-directory-history-core/04-RESEARCH.md
@.planning/phases/04-directory-history-core/04-01-PLAN.md

@internal/adapters/ui/file_browser/remote_pane.go
@internal/adapters/ui/file_browser/file_browser.go
</context>

<interfaces>
<!-- 需要修改的现有代码签名和结构 -->

From internal/adapters/ui/file_browser/remote_pane.go (当前代码):

```go
type RemotePane struct {
    *tview.Table
    log          *zap.SugaredLogger
    sftpService  ports.SFTPService
    server       domain.Server
    currentPath  string
    sortMode     FileSortMode
    showHidden   bool
    selected     map[string]bool
    connected    bool
    onPathChange func(path string)
    onFileAction func(fi domain.FileInfo)
}

// NavigateToParent — line 276-288, 缺少 onPathChange 调用
func (rp *RemotePane) NavigateToParent() {
    if !rp.connected { return }
    parent := parentPath(rp.currentPath)
    if parent == rp.currentPath { return }
    rp.currentPath = parent
    rp.selected = make(map[string]bool)
    rp.Refresh()
    // BUG: 缺少 onPathChange 回调
}

// NavigateInto — line 291-301, 已有 onPathChange 调用
func (rp *RemotePane) NavigateInto(dirName string) {
    if !rp.connected { return }
    rp.currentPath = joinPath(rp.currentPath, dirName)
    rp.selected = make(map[string]bool)
    rp.Refresh()
    if rp.onPathChange != nil {
        rp.onPathChange(rp.currentPath)
    }
}
```

From internal/adapters/ui/file_browser/file_browser.go (当前代码):

```go
type FileBrowser struct {
    *tview.Flex
    app            *tview.Application
    log            *zap.SugaredLogger
    fileService    ports.FileService
    sftpService    ports.SFTPService
    transferSvc    ports.TransferService
    server         domain.Server
    localPane      *LocalPane
    remotePane     *RemotePane
    statusBar      *tview.TextView
    transferModal  *TransferModal
    activePane     int
    transferring   bool
    transferCancel context.CancelFunc
    onClose        func()
}

// build() lines 126-133 — 现有 onPathChange 回调注册
fb.localPane.OnPathChange(func(_ string) {
    fb.app.Sync()
})
fb.remotePane.OnPathChange(func(_ string) {
    fb.app.Sync()
})
```

From Plan 01 创建的 RecentDirs (04-01-PLAN.md):
```go
type RecentDirs struct {
    *tview.Box
    paths   []string
    visible bool
}

func NewRecentDirs() *RecentDirs
func (rd *RecentDirs) Record(path string)
func (rd *RecentDirs) GetPaths() []string
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 修复 NavigateToParent bug 并添加 NavigateTo 方法</name>
  <files>internal/adapters/ui/file_browser/remote_pane.go</files>

  <read_first>
  - internal/adapters/ui/file_browser/remote_pane.go — 当前 NavigateToParent (line 276-288) 和 NavigateInto (line 291-301) 的完整代码，确认 bug 位置和 onPathChange 调用模式
  - .planning/phases/04-directory-history-core/04-CONTEXT.md — D-08 (NavigateToParent 修复) 和 D-09 (NavigateTo 方法) 决策
  - .planning/phases/04-directory-history-core/04-RESEARCH.md — Pattern 4 (NavigateToParent 修复) 和 Pattern 5 (NavigateTo 方法) 的完整代码示例
  </read_first>

  <action>
    修改 `internal/adapters/ui/file_browser/remote_pane.go` 中的两处：

    **修改 1: 修复 NavigateToParent（D-08, AUX-02）**

    在 `NavigateToParent()` 方法的 `rp.Refresh()` 调用之后（当前 line 287 之后），添加 onPathChange 回调调用。修改后的方法：

    ```go
    // NavigateToParent goes up one directory level.
    func (rp *RemotePane) NavigateToParent() {
        if !rp.connected {
            return
        }
        // Simple path splitting for remote paths (Unix-style)
        parent := parentPath(rp.currentPath)
        if parent == rp.currentPath {
            return // already at root
        }
        rp.currentPath = parent
        rp.selected = make(map[string]bool)
        rp.Refresh()
        if rp.onPathChange != nil {
            rp.onPathChange(rp.currentPath)
        }
    }
    ```

    设计原理: 与 NavigateInto (line 298-299) 保持对称。两个方法都修改 currentPath 并调用 Refresh，因此都应该通知观察者。修复后 `app.Sync()` 在返回上级时也被调用，解决 kitty 透明背景下的 ghost artifact 问题。

    **修改 2: 添加 NavigateTo 方法（D-09）**

    在 `NavigateInto()` 方法之后添加新方法：

    ```go
    // NavigateTo sets the remote pane to a specific path directly.
    // Unlike NavigateInto, it does not trigger onPathChange callback.
    // Used by RecentDirs popup navigation (Phase 5) to avoid re-recording
    // the path when the user selects from the recent list.
    func (rp *RemotePane) NavigateTo(path string) {
        if !rp.connected {
            return
        }
        rp.currentPath = path
        rp.selected = make(map[string]bool)
        rp.Refresh()
    }
    ```

    设计原理: Phase 5 的弹出列表选择跳转时不应触发路径记录（路径已在列表中）。NavigateTo 与 NavigateInto 的区别仅在于不调用 onPathChange。Refresh() 内部调用 populateTable()，populateTable() 末尾调用 UpdateTitle()，所以不需要单独调用 UpdateTitle()。

    **不要修改的其他部分:**
    - 不修改 NavigateInto()（已有 onPathChange 调用，行为正确）
    - 不修改 build() 中的 SetInputCapture（h 键处理已调用 NavigateToParent，修复后自动生效）
    - 不修改 RemotePane struct 定义
  </action>

  <verify>
    <automated>cd /home/huanglin/code/lazyssh && go build ./... 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - `remote_pane.go` 包含 `func (rp *RemotePane) NavigateTo(path string) {`
    - NavigateToParent 方法体内包含 `if rp.onPathChange != nil {` 和 `rp.onPathChange(rp.currentPath)` （在 Refresh() 之后）
    - NavigateTo 方法体内不包含 `onPathChange` 调用
    - NavigateTo 方法体内包含 `rp.currentPath = path` 和 `rp.Refresh()`
    - `go build ./...` 编译通过
  </acceptance_criteria>

  <done>
    - NavigateToParent 在 Refresh() 后调用 onPathChange 回调（AUX-02 修复完成）
    - NavigateTo(path) 方法已添加，直接设置路径并刷新，不触发 onPathChange
    - 编译通过，无类型错误
  </done>
</task>

<task type="auto">
  <name>Task 2: 在 FileBrowser 中接入 RecentDirs 并连线 Record 调用</name>
  <files>internal/adapters/ui/file_browser/file_browser.go</files>

  <read_first>
  - internal/adapters/ui/file_browser/file_browser.go — FileBrowser struct 定义 (line 40-56) 和 build() 方法 (line 83-202)，特别是 onPathChange 回调注册 (line 126-133)
  - internal/adapters/ui/file_browser/remote_pane.go — NavigateToParent 和 NavigateInto 的 onPathChange 调用点（确认 Task 1 修复后的代码）
  - .planning/phases/04-directory-history-core/04-CONTEXT.md — D-03 (RecentDocs 字段位置) 和 D-04 (Record 调用位置) 决策
  - .planning/phases/04-directory-history-core/04-RESEARCH.md — Pattern 3 (Callback Registration in build()) 的代码示例
  </read_first>

  <action>
    修改 `internal/adapters/ui/file_browser/file_browser.go` 中的两处：

    **修改 1: 在 FileBrowser struct 中添加 recentDirs 字段**

    在 `transferModal *TransferModal` 字段之后添加：
    ```go
    type FileBrowser struct {
        *tview.Flex
        app            *tview.Application
        log            *zap.SugaredLogger
        fileService    ports.FileService
        sftpService    ports.SFTPService
        transferSvc    ports.TransferService
        server         domain.Server
        localPane      *LocalPane
        remotePane     *RemotePane
        statusBar      *tview.TextView
        transferModal  *TransferModal
        recentDirs     *RecentDirs  // NEW: in-memory MRU list of recent remote directories
        activePane     int
        transferring   bool
        transferCancel context.CancelFunc
        onClose        func()
    }
    ```

    **修改 2: 在 build() 方法中初始化 RecentDirs 并修改 RemotePane 的 onPathChange 回调**

    在 `fb.transferModal = NewTransferModal(fb.app)` 行（约 line 103）之后添加 RecentDirs 初始化：
    ```go
    // Create recent directories tracker (Phase 4: data layer, Phase 5: popup UI)
    fb.recentDirs = NewRecentDirs()
    ```

    修改 RemotePane 的 onPathChange 回调（当前 line 131-133），将 Record 调用添加到回调中：
    ```go
    fb.remotePane.OnPathChange(func(path string) {
        fb.app.Sync()
        fb.recentDirs.Record(path) // D-04: record path for recent dirs list
    })
    ```

    设计原理: 遵循 D-04 决策，通过 onPathChange 回调统一记录路径。Task 1 修复了 NavigateToParent 的 onPathChange 缺失问题，现在两种导航方式（NavigateInto 和 NavigateToParent）都会触发此回调，从而将路径记录到 RecentDirs。回调参数从 `_ string` 改为 `path string`，因为 Record 需要使用路径值。

    **不要修改的其他部分:**
    - 不修改 LocalPane 的 onPathChange 回调（本地路径不需要记录到远程目录历史）
    - 不修改 FileBrowser.Draw()（Phase 5 才添加 overlay Draw 调用）
    - 不修改 handleGlobalKeys（Phase 5 才添加 `r` 键处理）
  </action>

  <verify>
    <automated>cd /home/huanglin/code/lazyssh && go build ./... 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - `file_browser.go` FileBrowser struct 包含 `recentDirs *RecentDirs` 字段
    - `file_browser.go` build() 方法包含 `fb.recentDirs = NewRecentDirs()`
    - `file_browser.go` RemotePane 的 onPathChange 回调参数从 `_ string` 改为 `path string`
    - `file_browser.go` RemotePane 的 onPathChange 回调体内包含 `fb.recentDirs.Record(path)`
    - LocalPane 的 onPathChange 回调未被修改（参数仍为 `_ string`，无 Record 调用）
    - `go build ./...` 编译通过
  </acceptance_criteria>

  <done>
    - FileBrowser struct 包含 recentDirs 字段
    - build() 中 RecentDirs 被初始化
    - RemotePane 的 onPathChange 回调同时执行 app.Sync() 和 recentDirs.Record(path)
    - NavigateToParent 触发的路径变更（Task 1 修复后）会被 Record() 记录（HIST-01 完整覆盖）
    - NavigateInto 触发的路径变更继续被 Record() 记录（已有行为）
    - 编译通过
  </done>
</task>

</tasks>

<verification>
1. `go build ./...` 编译通过
2. `go test ./internal/adapters/ui/file_browser/ -run TestRecord -v` 测试仍然通过（确认 Plan 01 未被破坏）
3. `remote_pane.go` NavigateToParent 方法包含 `rp.onPathChange(rp.currentPath)` 调用
4. `remote_pane.go` NavigateTo 方法存在且不包含 `onPathChange` 调用
5. `file_browser.go` FileBrowser struct 包含 `recentDirs *RecentDirs`
6. `file_browser.go` build() 中 RemotePane 的 onPathChange 回调包含 `fb.recentDirs.Record(path)`
7. `file_browser.go` build() 中 LocalPane 的 onPathChange 回调未被修改
</verification>

<success_criteria>
- NavigateToParent bug 修复完成（AUX-02）—— 返回上级时 onPathChange 被调用，app.Sync() 被执行
- 所有远程目录导航事件（NavigateInto + NavigateToParent）都通过 onPathChange 回调触发 Record()（HIST-01）
- NavigateTo 方法可用但不触发 onPathChange（D-09，Phase 5 消费）
- Record 调用仅接线到 RemotePane 的 onPathChange，不影响 LocalPane
- 零新外部依赖
</success_criteria>

<output>
After completion, create `.planning/phases/04-directory-history-core/04-02-SUMMARY.md`
</output>
