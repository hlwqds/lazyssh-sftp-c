---
phase: 04-directory-history-core
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/adapters/ui/file_browser/recent_dirs.go
  - internal/adapters/ui/file_browser/recent_dirs_test.go
autonomous: true
requirements: [HIST-01, HIST-02, HIST-03, HIST-04]
user_setup: []

must_haves:
  truths:
    - "Record(path) 将路径规范化（去尾部斜杠）后存入列表头部"
    - "Record(path) 对已存在的路径执行 move-to-front 去重，列表中仅保留一份且位于头部"
    - "Record(path) 跳过以 '.' 开头的相对路径（如 '.'、'./docs'）"
    - "GetPaths() 返回按 MRU 顺序排列的路径 slice"
    - "列表始终不超过 10 条记录，超出时最旧的条目被截断"
    - "空列表上调用 Record() 正常工作（nil slice 安全）"
  artifacts:
    - path: "internal/adapters/ui/file_browser/recent_dirs.go"
      provides: "RecentDirs struct, Record(), GetPaths(), maxRecentDirs constant"
      min_lines: 50
      contains: "func (rd *RecentDirs) Record("
    - path: "internal/adapters/ui/file_browser/recent_dirs_test.go"
      provides: "Record() 行为的单元测试"
      min_lines: 80
      contains: "func TestRecord"
  key_links:
    - from: "recent_dirs.go"
      to: "file_browser.go"
      via: "fb.recentDirs.Record(path) 调用"
      pattern: "recentDirs\\.Record\\("
---

<objective>
创建 RecentDirs 内存 MRU（Most Recently Used）数据结构，实现路径记录、去重和截断逻辑。

Purpose: 为 Phase 5 的弹出列表 UI 提供数据层基础。RecentDirs 是一个纯内存数据结构，不涉及 UI 渲染或外部依赖。

Output: `recent_dirs.go`（RecentDirs struct + Record + GetPaths）和 `recent_dirs_test.go`（单元测试）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-directory-history-core/04-CONTEXT.md
@.planning/phases/04-directory-history-core/04-RESEARCH.md

@internal/adapters/ui/file_browser/transfer_modal.go
</context>

<interfaces>
<!-- TransferModal overlay 模式模板 —— RecentDirs 应遵循相同结构模式 -->

From internal/adapters/ui/file_browser/transfer_modal.go:
```go
type TransferModal struct {
    *tview.Box
    app *tview.Application
    // ...
    visible   bool
    onDismiss func()
}
```

关键模式:
- 嵌入 `*tview.Box`（通过 `tview.NewBox()` 创建）
- `visible` 布尔标志控制显示
- `Show()` / `Hide()` / `IsVisible()` 方法
- `Draw(screen tcell.Screen)` 手动渲染（Phase 4 仅提供空壳，Phase 5 实现渲染）
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 RecentDirs 数据结构并编写单元测试</name>
  <files>internal/adapters/ui/file_browser/recent_dirs.go, internal/adapters/ui/file_browser/recent_dirs_test.go</files>

  <read_first>
  - internal/adapters/ui/file_browser/transfer_modal.go — 作为 overlay 组件的结构模板（嵌入 *tview.Box, visible flag, Show/Hide/IsVisible 模式）
  - .planning/phases/04-directory-history-core/04-CONTEXT.md — D-01 到 D-09 锁定决策
  - .planning/phases/04-directory-history-core/04-RESEARCH.md — Record 伪代码、边界条件分析
  </read_first>

  <behavior>
    - Test 1: Record("/home/user/docs") 后 GetPaths() 返回 ["/home/user/docs"]
    - Test 2: 连续 Record 3 个不同路径后，GetPaths() 返回按 MRU 顺序排列的 3 条路径
    - Test 3: Record("/a") 后 Record("/b") 后再次 Record("/a")，GetPaths() 返回 ["/a", "/b"]（去重 + move-to-front）
    - Test 4: Record 11 个不同路径后，GetPaths() 返回 10 条（最旧的被截断）
    - Test 5: Record(".") 和 Record("./docs") 后 GetPaths() 返回空（相对路径被跳过）
    - Test 6: Record("/home/user/") 后 GetPaths() 返回 ["/home/user"]（尾部斜杠被去除）
    - Test 7: 空 RecentDirs 上调用 GetPaths() 返回空 slice（nil 安全）
    - Test 8: Record("/home/user") 后 Record("/home/user")，GetPaths() 返回 ["/home/user"]（重复调用不产生重复条目）
  </behavior>

  <action>
    在 `internal/adapters/ui/file_browser/` 目录下创建两个新文件：

    **1. `recent_dirs.go`**

    遵循 D-03 决策：RecentDirs 作为独立 struct，嵌入 `*tview.Box`（与 TransferModal 一致）。

    结构定义:
    ```go
    const maxRecentDirs = 10

    // RecentDirs maintains an in-memory MRU (Most Recently Used) list of remote directory paths.
    // It follows the TransferModal overlay pattern: embeds *tview.Box, uses visible flag.
    // Phase 4 provides the data layer; Phase 5 adds Draw() rendering.
    type RecentDirs struct {
        *tview.Box
        paths   []string
        visible bool
    }
    ```

    构造函数:
    ```go
    func NewRecentDirs() *RecentDirs {
        rd := &RecentDirs{
            Box:    tview.NewBox(),
            paths:  make([]string, 0, maxRecentDirs),
            visible: false,
        }
        rd.SetBorder(true).
            SetBorderColor(tcell.Color238).
            SetTitleColor(tcell.Color250).
            SetBackgroundColor(tcell.Color232)
        return rd
    }
    ```

    Record 方法（严格遵循 D-01, D-02, D-05, D-06 决策）:
    ```go
    // Record adds a path to the MRU list. If the path already exists, it is moved to the front.
    // Relative paths (starting with ".") are silently skipped.
    // Paths are normalized by removing trailing slashes.
    // The list is capped at maxRecentDirs entries; oldest entries are truncated.
    func (rd *RecentDirs) Record(path string) {
        normalized := strings.TrimRight(path, "/")
        if strings.HasPrefix(normalized, ".") {
            return
        }
        for i, p := range rd.paths {
            if p == normalized {
                rd.paths = append(rd.paths[:i], rd.paths[i+1:]...)
                break
            }
        }
        rd.paths = append([]string{normalized}, rd.paths...)
        if len(rd.paths) > maxRecentDirs {
            rd.paths = rd.paths[:maxRecentDirs]
        }
    }
    ```

    GetPaths 方法:
    ```go
    // GetPaths returns a copy of the MRU path list (most recent first).
    func (rd *RecentDirs) GetPaths() []string {
        result := make([]string, len(rd.paths))
        copy(result, rd.paths)
        return result
    }
    ```

    Overlay 骨架方法（Phase 5 实现具体渲染）:
    ```go
    func (rd *RecentDirs) Draw(screen tcell.Screen) {
        if !rd.visible {
            return
        }
        rd.Box.DrawForSubclass(screen, rd)
        // Phase 5: 渲染目录列表
    }

    func (rd *RecentDirs) Show() { rd.visible = true }
    func (rd *RecentDirs) Hide() { rd.visible = false }
    func (rd *RecentDirs) IsVisible() bool { return rd.visible }
    ```

    import 需要: `"strings"`, `"github.com/gdamore/tcell/v2"`, `"github.com/rivo/tview"`

    **2. `recent_dirs_test.go`**

    包名: `file_browser`（与源文件同包，可直接访问私有成员）

    测试用例实现 `<behavior>` 中列出的 8 个场景。每个测试创建 `NewRecentDirs()` 实例，调用 `Record()` 和 `GetPaths()`，使用标准库 `testing` 包断言结果。

    不需要 mock 任何外部依赖——RecentDirs 是纯内存数据结构。

    **重要:** 不要引入任何新依赖。仅使用 Go 标准库 `testing` 包。
  </action>

  <verify>
    <automated>cd /home/huanglin/code/lazyssh && go test ./internal/adapters/ui/file_browser/ -run TestRecord -v</automated>
  </verify>

  <done>
    - `recent_dirs.go` 存在，包含 RecentDirs struct, Record(), GetPaths(), maxRecentDirs=10
    - `recent_dirs_test.go` 存在，包含 8 个测试用例覆盖所有行为
    - `go test ./internal/adapters/ui/file_browser/ -run TestRecord` 全部通过
    - Record 方法实现 move-to-front 去重（D-02）
    - Record 方法跳过 "." 开头的相对路径（D-05）
    - Record 方法路径规范化去尾部斜杠（D-06）
    - 列表上限 10 条（D-01, HIST-04）
  </done>
</task>

</tasks>

<verification>
1. `go build ./...` 编译通过（无循环依赖、无类型错误）
2. `go test ./internal/adapters/ui/file_browser/ -run TestRecord -v` 全部通过
3. `recent_dirs.go` 包含 `maxRecentDirs = 10` 常量
4. `recent_dirs.go` 包含 `func (rd *RecentDirs) Record(path string)` 方法
5. `recent_dirs.go` 包含 `func (rd *RecentDirs) GetPaths() []string` 方法
6. Record 方法内包含 `strings.HasPrefix(normalized, ".")` 相对路径过滤
7. Record 方法内包含 `strings.TrimRight(path, "/")` 路径规范化
</verification>

<success_criteria>
- RecentDirs struct 嵌入 `*tview.Box`，遵循 TransferModal overlay 模式
- Record() 正确实现 MRU 排序（HIST-02）、去重（HIST-03）、截断（HIST-04）
- Record() 跳过相对路径（HIST-01 的隐含要求，防止 "." 被记录）
- GetPaths() 返回 MRU 顺序的路径副本
- 8 个单元测试全部通过
- 零新外部依赖
</success_criteria>

<output>
After completion, create `.planning/phases/04-directory-history-core/04-01-SUMMARY.md`
</output>
